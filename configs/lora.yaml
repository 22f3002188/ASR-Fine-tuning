lora:
  enabled: true
  r: 32
  lora_alpha: 64
  target_modules:
    - q_proj
    - v_proj
  lora_dropout: 0.05
  bias: none
  apply_to: decoder_only

freeze:
  encoder: true
  freeze_decoder_layers: 0
  freeze_embed_positions: true